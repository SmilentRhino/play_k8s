kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: grafana-claim
  namespace: "{{ data.namespace }}" 
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: gp2
